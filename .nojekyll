Yeah... Weird I know, but this has a purpose, since I use some 3rd party plugins, GH-Pages will not run then, and that will make a mess, so instead I build the website locally and deploy the contents "manually"